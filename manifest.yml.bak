applications:
- name: jenkins
  memory: 2048M
  buildpack: java3-agent
  services:
    ${name}-fs:
      type: filesystem
  stackato:
    hooks:
      pre-running:
      - sh stackato-setup.sh
  timeout: 360
